<% if (site.tags.length) { %>
    <div class="widget-wrap">
        <h3 class="widget-title widget-title-tag"><i class="material-icons">star_border</i><%= __('tags') %></h3>
        <div class="widget">
            <%- list_tags({show_count: theme.show_count}) %>
        </div>
    </div>
<% } %>
